# Production environment variables for Vercel deployment
# These ensure the build completes successfully even with strict CI checks

# Disable CI mode to prevent warnings being treated as errors
CI=false
