{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid",
        "profile": {
          ".validate": "newData.hasChildren(['name', 'email']) || newData.val() === null"
        },
        "settings": {
          ".validate": "newData.isObject() || newData.val() === null"
        },
        "stats": {
          ".validate": "newData.isObject() || newData.val() === null"
        },
        "bills": {
          "$billId": {
            ".validate": "newData.hasChild('billNumber') || newData.val() === null"
          }
        },
        "documents": {
          "$docId": {
            ".validate": "newData.isObject() || newData.val() === null"
          }
        },
        "gstForms": {
          "$formId": {
            ".validate": "newData.isObject() || newData.val() === null"
          }
        },
        "reports": {
          "$reportId": {
            ".validate": "newData.isObject() || newData.val() === null"
          }
        }
      }
    },
    "reminders": {
      ".read": "auth != null",
      ".write": "auth != null",
      "$reminderId": {
        ".read": "root.child('reminders').child($reminderId).child('userId').val() === auth.uid",
        ".write": "root.child('reminders').child($reminderId).child('userId').val() === auth.uid",
        ".validate": "newData.hasChildren(['userId', 'type', 'title']) || newData.val() === null"
      }
    },
    "test": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}
